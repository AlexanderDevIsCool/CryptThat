<% provide(:title, 'Your files') %>

<ul class="users">
  <% @files.each do |file| %>
    <li>
      <%= file.filename %>
      | <%= link_to "delete", files_upload_destroy_path(file.id), method: :delete,
                    data: { confirm: "You sure?" } %>
    </li>
  <% end %>
</ul>
