<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to 'CryptThat', root_path, class: 'navbar-brand' %>
    </div>
    <div class="nav-buttons">
      <% if logged_in? %>
        <% if current_user.admin? %>
          <%= link_to "Users", users_path, class: "btn btn-info navbar-btn" %>
        <% end %>
        <%= link_to "My Files", current_user,
                        class: "btn btn-success navbar-btn" %>
        <%= link_to "Log out", logout_path,
                        method: :delete, class: "btn btn-danger navbar-btn" %>
      <% else %>
        <%= link_to 'Log in', login_path, class: "btn btn-success navbar-btn" %>
        <%= link_to 'Sign up', signup_path, class: "btn btn-warning navbar-btn" %>>
      <% end %>
    </div>
  </div>
</nav>
